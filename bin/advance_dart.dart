import 'list.dart';

void main(List<String> arguments) async {
  // await setAndList().then((value) => print(value));
  //  print(input());
  // Set<int> set = {1, 2, 3, 4, 5};
  learnListFillRange();
}
